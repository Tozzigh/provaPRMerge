import React from "react";

const Footer = () => {
	return (
		<footer className="footer sticky-bottom py-3 bg-dark">
            <div className="container">
                <span className="text-muted">Place sticky footer content here.</span>
            </div>
        </footer>
    );
};

export default Footer;
